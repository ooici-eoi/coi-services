driver_config :
  dvr_mod : ion.agents.data.handlers.ruv_data_handler
  dvr_cls : RuvDataHandler
  dh_cfg :
    TESTING : True
    max_records : 50
    ## CONFIGURED DURING dreg.make_agent_instance
    # stream_id : ''
    # data_producer_id : ''
    # taxonomy : ''
## CONFIGURED DURING dreg.make_agent_instance OR dams.start_external_dataset_agent_instance
# stream_config : {}
agent : {resource_id : ''} # resource_id filled during creation
test_mode : True
